#the rectangle module contaisn functions related
#to rectangle calculations

def area(width, length):
    #area accept integers for width and alength
    #it calculates and returns the area of  arectangle
    
    return width * length

def perimeter(width, length):
    #perimeter accepts integers for width and length
    #it calculates and returns the perimeter of a rectangle
    
    return (width * 2) + (length * 2)
    